PLACE YOUR LOGO HERE
====================

The website references a logo image at:  assets/logo.jpg

ACTION NEEDED:
1. Save the Advik Learning Centre logo (the Shiva + open book image you shared)
   into this "assets" folder.
2. Name it exactly:  logo.jpg
   (If you save it as a PNG instead, name it logo.png and update the
    references in the HTML files from "assets/logo.jpg" to "assets/logo.png".)

TIP: A version of the logo with a TRANSPARENT background (PNG) will look
cleaner in the navbar and footer. The square parchment version also works fine.

Optional images you can add later (then reference them in the HTML):
- hero/classroom photos
- faculty photos
- achiever photos for the Results page
